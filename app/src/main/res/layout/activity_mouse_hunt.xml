<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A2E1A">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/mouseScoreText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Caught: 0 🐭"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/mouseTimerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⏱ 30s"
            android:textSize="20sp"
            android:textColor="#AAFFAA" />

    </LinearLayout>

    <!-- Game Canvas -->
    <com.imbir.game.ui.minigame.MouseHuntView
        android:id="@+id/mouseView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Footer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#0D1A0D">

        <TextView
            android:id="@+id/mouseStatusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap Start to hunt! 🐭"
            android:textSize="14sp"
            android:textColor="#AAFFAA"
            android:layout_marginBottom="8dp"
            android:layout_gravity="center" />

        <Button
            android:id="@+id/btnMouseStart"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Start Hunt"
            android:backgroundTint="#4CAF50"
            android:textColor="#FFFFFF"
            style="@style/Widget.Material3.Button" />

    </LinearLayout>

</LinearLayout>